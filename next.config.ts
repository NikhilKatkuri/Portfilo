import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
  domains:['cdn.jsdelivr.net','upload.wikimedia.org','static.expo.dev']
  }
};

export default nextConfig;
